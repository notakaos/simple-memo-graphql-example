# Develop

```bash
git clone <GIT_URL>
cd simple-memo-graphql-example
npm install
npm start
```

## PostgreSQL

```bash
# Start PostgreSQL (dev)
cd development-tools/postgresql-docker
docker-compose up -d

# Stop PostgreSQL (dev)
# cd development-tools/postgresql-docker
docker-compose down
```